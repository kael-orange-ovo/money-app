# CLAUDE.md — 记账 app 项目约定

> 本文件是 Claude Code 每次工作的"项目说明书"。Claude 在开发和交流中必须遵守以下约定。

## 项目概况

- **项目名称**：记账 app
- **目标**：一个桌面记账应用，运行在 Windows 和 Mac 电脑上，记录用户的每一次人民币花销。
- **目标用户**：个人用户（用户本人），用于日常个人/家庭开支记录。

## ⚠️ 最重要的工作约定（必须全程遵守）

1. **用户是零基础小白**：用户不懂编程，也是第一次使用 Claude Code。所有交流必须用通俗易懂的语言，避免堆砌术语；涉及专业名词时必须解释。

2. **技术方案必须由 Claude 列出、由用户决定**：
   - 任何技术选型（技术栈、框架、库、数据结构、界面方案、命名等）Claude **不得擅自决定**。
   - Claude 必须先**列出多个方案**，并**用大白话解释每个方案的优劣势**，然后由**用户挑选决定**。
   - 即使用户说"你决定吧"，也要至少给出 2 个方案 + 推荐，等用户确认后再动手。
   - 只有当用户明确说"就按你推荐的来"或直接选中某方案，才能开始实现。

3. **小步确认，不要一次做太多**：每个阶段先出方案/文档让用户确认，确认后再进入下一步。避免一次写大量代码后用户看不懂。

4. **功能需求也按同样原则**：功能设计（如分类体系、界面布局）也先列方案给用户选，不要想当然。

## 如何运行与开发

- **启动开发模式（日常看效果）**：在项目根目录运行 `npm run dev`，会弹出软件窗口，改代码后自动刷新。
- **编译打包**：`npm run build`；打 Windows 安装包 `npm run build:win`；打 Mac 安装包 `npm run build:mac`。

### ⚠️ 通过 Claude Code 启动时的坑

Claude Code 的沙箱环境会自动设置 `ELECTRON_RUN_AS_NODE=1`，这会让 Electron 不弹窗口、`require('electron')` 返回二进制路径字符串而不是 API，导致主进程报 `Cannot read properties of undefined (reading 'getPath')`。

**解决办法**：通过 Claude Code 启动 Electron 应用时，必须：
1. 取消该变量：`env -u ELECTRON_RUN_AS_NODE npm run dev`；
2. 并关闭沙箱（Bash 工具用 `dangerouslyDisableSandbox: true`），窗口才能正常显示。

（用户自己在终端里运行 `npm run dev` 时不受影响，因为那时没有这个变量。）

## 已确定的需求

1. 运行平台：Windows + Mac 桌面电脑。
2. 核心功能：记录每一次花销（人民币），并记录花销的分类；**同时也要记录收入**。
3. 分类为 **2 级**：一级大类 + 二级小类。支出分类已确认；收入分类待用户确认。
4. 金额单位：人民币（¥）。
5. **技术栈：Electron + Vue 3**（2026-09-09 用户选定）。
6. 预算提醒：暂不做。
7. 整体布局：左侧导航（记账 / 账单 / 统计），浅色清爽配色。
8. 数据存储：JSON 文件（本地纯文本，好备份、好查看）。

## 项目状态

- [x] 产品文档初稿（见 `docs/产品文档.md`）
- [x] 技术栈选型（Electron + Vue 3，用户已选）
- [x] 支出分类体系（用户已确认）
- [x] 收入分类（用户已确认：工资/理财/人情/其他）
- [x] 界面设计（左侧导航 + 浅色配色，见 `docs/界面设计.md`）
- [x] 数据存储方式（JSON 文件，用户已选）
- [x] 编码实现（第一版：记一笔 / 账单 / 统计 已可运行）
- [x] Windows 安装包（`dist/记账app Setup 0.1.0.exe`，2026-09-09 已打包）
- [x] 应用图标（蓝底白 ¥，源文件 `scripts/icon.svg`，由 `node scripts/make-icon.js` 生成 `build/icon.png`）
- [ ] Mac 安装包（需在 Mac 电脑上运行 `npm run build:mac`，Windows 无法打 Mac 包）

## 下一步待办

1. 用户试用第一版，反馈要调整的地方。
2. （可选）换应用图标。
3. 用户有 Mac 电脑后，在 Mac 上打 Mac 安装包。
